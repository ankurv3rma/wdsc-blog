{{!< default}}


{{!-- <header class="site-header outer {{#if @blog.cover_image}}" style="background-image: url({{@blog.cover_image}}){{else}}no-cover{{/if}}">
    <div class="inner">
        <div class="site-header-content">
            <h1 class="site-title">
                {{#if @blog.logo}}
                    <img class="site-logo" src="{{@blog.logo}}" alt="{{@blog.title}}" />
                {{else}}
                    {{@blog.title}}
                {{/if}}
            </h1>
            <h2 class="site-description">{{@blog.description}}</h2>
        </div>
    </div>
</header> --}}

<main id="site-main" class="site-main outer">
    <div class="highlight-feed">
      
        <div class="inner">

            <div class="site-section">
                <div class="post-feed highlight-feed-primary">
                    {{#get "posts" filter="featured:true" include="authors,tags" limit="1"}}
                    {{#foreach posts}}
                        {{> "post-card"}}
                    {{/foreach}}
                    {{/get}}
                </div>
                {{#get "posts" filter="featured:true" include="authors,tags" limit="3"}}
                <div class="post-feed highlight-feed-secondary">
                    {{#foreach posts}}
                        {{> "post-card"}}
                    {{/foreach}}
                </div>
                {{/get}}
            </div>

        </div>

    </div>

    {{!-- <div class="breakout-section">
        <div class="inner">
            CTA
        </div>
    </div> --}}

    <div class="inner">

        {{#get "posts" filter="tag:tag-2" include="authors,tags" limit="3"}}
        <div class="site-section">
            <header class="site-section-header">
                <h2 class="site-section-title">Tag 1</h2>
                <a class="site-section-morelink" href="{{@blog.url}}/tag/tag-1/">See more {{> "icons/arrow-right-small"}}</a>
            </header>
            <div class="post-feed">
                {{#foreach posts}}
                    {{> "post-card"}}
                {{/foreach}}
            </div>
        </div>
        {{/get}}

        {{#get "posts" filter="tag:tag-2" include="authors,tags" limit="3"}}
        <div class="site-section">
            <header class="site-section-header">
                <h2 class="site-section-title">Tag 2</h2>
                <a class="site-section-morelink" href="{{@blog.url}}/tag/tag-2/">See more {{> "icons/arrow-right-small"}}</a>
            </header>
            <div class="post-feed">
                {{#foreach posts}}
                    {{> "post-card"}}
                {{/foreach}}
            </div>
        </div>
        {{/get}}
        {{#get "posts" filter="tag:tag-3" include="authors,tags" limit="3"}}
        <div class="site-section">
            <header class="site-section-header">
                <h2 class="site-section-title">Tag 3</h2>
                <a class="site-section-morelink" href="{{@blog.url}}/tag/tag-3/">See more {{> "icons/arrow-right-small"}}</a>
            </header>
            <div class="post-feed">
                {{#foreach posts}}
                    {{> "post-card"}}
                {{/foreach}}
            </div>
        </div>
        {{/get}}

        {{!-- <div class="post-feed">
            {{#foreach posts}}

                {{> "post-card"}}

            {{/foreach}}
        </div> --}}

    </div>
</main>
